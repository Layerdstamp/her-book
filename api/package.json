{
  "name": "puppy-record-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute puppy-record --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute puppy-record --local --file=./schema.sql"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
